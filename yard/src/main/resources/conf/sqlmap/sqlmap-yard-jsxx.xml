<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">


    <select id="getJsxx" paramterClass="map" resultMap="map">
        select * from jsxx
    </select>

    <insert id="insertJsxx" paramterClass="map" resultMap="map">
        insert into jsxx ()
        values ()
    </insert>

    <update id="updateJsxx" paramterClass="map" resultMap="map">
        update jsxx t set

        where t.jsid = #JSID#
    </update>

    <delete id="delJsxx" paramterClass="map" resuleMap="map">
        delete from jsxx t where t.jsid = #JSID#
    </delete>

</beans>